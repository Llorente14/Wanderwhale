import 'package:flutter/widgets.dart';

class FlightRecommendation extends StatelessWidget {
  const FlightRecommendation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
